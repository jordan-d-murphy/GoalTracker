@model IEnumerable<GoalTracker.Models.Roadmap>

@{
    ViewData["Title"] = "Index";
}

<h1>Roadmap Index</h1>

<p>
    <a asp-action="Create">Create New Roadmap</a>
</p>


@foreach (var item in Model)
{
    <div class="row">

    <div class="card mb-3">
        <div class="card-body">
            <h5 class="card-title">@item.Title</h5>
            <h6 class="card-subtitle mb-2 text-muted">@item.Category</h6>
            <p class="card-text">@item.Description</p>

            <div class="row position-relative p-3 m-3" style="width: 100%;">

                @for (int i = 1, y = new Random().Next(1, 7); i < 8; i++)
                    {
                        <div class="progress position-absolute start-0" style="top: 45% !important; height: 4px; width: 90%;">
                            <div class="progress-bar" role="progressbar" style="width: @((int)((y/7m)*100))%"></div>
                        </div>
                        if (i < y)
                        {
                            <div class="col z-5k">
                                <div class="roadmap roadmap-complete px-3 py-4 mb-2 bg-body-secondary text-center rounded">
                                    <i class="bi-check2-square" style="font-size: 2rem; color: #0077cc;"></i>
                                </div>
                            </div>
                        }
                        else if (i == y) 
                        {
                            <div class="col z-5k">
                                <div class="roadmap roadmap-create px-3 py-4 mb-2 bg-body-secondary text-center rounded">
                                    <i class="bi-plus-square" style="font-size: 2rem; color: #0077cc;"></i>
                                </div>
                            </div>
                        }
                        else 
                        {
                            <div class="col z-5k">
                                <div class="roadmap px-3 py-4 mb-2 bg-body-secondary text-center rounded">
                                    <i class="bi-square" style="font-size: 2rem; color: #0077cc; visibility: hidden;"></i>
                                </div>
                            </div>
                        }
                    }
                </div>

                <a href="#" class="card-link">Card link</a>
                <a href="#" class="card-link">Another link</a>
            </div>
        </div>

    </div>

}
